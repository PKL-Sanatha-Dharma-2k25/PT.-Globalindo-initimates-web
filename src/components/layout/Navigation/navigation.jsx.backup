import React, { useState, useEffect } from "react";

// ========================================
// IMPORT DESIGN TOKENS (Design System v2.0)
// ========================================
const tokens = {
  maxWidths: {
    content: 'max-w-7xl',
  },
  typography: {
    h1: 'text-2xl sm:text-3xl lg:text-4xl xl:text-5xl font-bold',
    h2: 'text-xl sm:text-2xl lg:text-3xl xl:text-4xl font-bold',
    h3: 'text-lg sm:text-xl lg:text-2xl font-bold',
    bodyLg: 'text-sm md:text-base lg:text-lg leading-relaxed',
    bodyBase: 'text-xs md:text-sm lg:text-base leading-relaxed',
    label: 'text-xs md:text-sm font-medium',
    caption: 'text-[10px] md:text-xs text-gray-500',
  },
  spacing: {
    container: {
      default: 'container mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl',
    },
  },
  buttons: {
    primary: {
      blue: 'btn-primary-blue',
      orange: 'btn-primary-orange',
    },
  },
  animations: {
    basic: {
      fadeIn: 'animate-fadeIn',
      slideUp: 'animate-slideUp',
    },
  },
  iconBox: {
    sm: 'w-7 h-7 md:w-8 md:h-8',
    md: 'w-8 h-8 md:w-9 md:h-9 lg:w-10 lg:h-10',
  },
};

const Navigation = () => {
  const [isOpen, setIsOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [activeSection, setActiveSection] = useState("home");
  const [language, setLanguage] = useState("EN");
  const [showLangMenu, setShowLangMenu] = useState(false);
  const [isScrollingDown, setIsScrollingDown] = useState(false);
  const [lastScrollY, setLastScrollY] = useState(0);

  useEffect(() => {
    const handleScroll = () => {
      const currentScrollY = window.scrollY;
      
      setScrolled(currentScrollY > 20);
      setIsScrollingDown(currentScrollY > lastScrollY && currentScrollY > 100);
      setLastScrollY(currentScrollY);
    };

    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, [lastScrollY]);

  const languages = [
    { code: "EN", name: "English", flag: "🇬🇧" },
    { code: "ID", name: "Indonesia", flag: "🇮🇩" },
    { code: "CN", name: "中文", flag: "🇨🇳" },
  ];

  const menuItems = [
    { id: "home", label: "Home" },
    { id: "about", label: "About" },
    { id: "market", label: "Market" },
    { id: "facilities", label: "Facilities" },
    { id: "product", label: "Product" },
    { id: "team", label: "Team" },
    { id: "csr", label: "CSR" },
    { id: "certificate", label: "Certificate" },
    { id: "contact", label: "Contact" },
  ];

  return (
    <>
      <nav 
        className={`fixed top-0 left-0 right-0 z-50 transition-all duration-500 ${
          isScrollingDown ? '-translate-y-full' : 'translate-y-0'
        } ${
          scrolled 
            ? 'bg-white/90 backdrop-blur-2xl shadow-xl border-b border-gray-200/50' 
            : 'bg-gradient-to-b from-black/40 via-black/20 to-transparent backdrop-blur-sm'
        }`}
      >
        <div className={tokens.spacing.container.default}>
          <div className="flex justify-between items-center h-16 sm:h-18">
            {/* Logo - Using iconBox tokens */}
            <div className="flex items-center gap-2 sm:gap-3 group cursor-pointer relative z-10">
              <div className="relative">
                {/* Glow Effect */}
                <div className={`absolute -inset-2 rounded-xl opacity-0 group-hover:opacity-20 blur-xl transition-all duration-700 ${
                  scrolled 
                    ? 'bg-gradient-to-r from-orange-500 to-pink-500' 
                    : 'bg-gradient-to-r from-white to-blue-200'
                }`}></div>
                
                {/* Icon - Using iconBox.md */}
                <img 
                  src="/images/icon.PNG"
                  alt="GI Icon" 
                  className={`${tokens.iconBox.md} w-auto relative z-10 transition-all duration-500 group-hover:scale-105 drop-shadow-lg`}
                />
              </div>
              
              {/* Brand Text - Using typography tokens */}
              <div className="flex flex-col leading-tight">
                <span className={`font-bold transition-colors duration-300 ${tokens.typography.bodyLg} ${
                  scrolled ? 'text-gray-900' : 'text-white'
                }`}>
                  PT. GLOBALINDO
                </span>
                <span className={`font-semibold tracking-wider transition-colors duration-300 ${tokens.typography.label} ${
                  scrolled ? 'text-orange-600' : 'text-orange-300'
                }`}>
                  INTIMATES
                </span>
              </div>
            </div>

            {/* Desktop Navigation - Using typography.label */}
            <div className="hidden lg:flex items-center gap-1 xl:gap-2">
              {menuItems.map((item, index) => (
                <a 
                  key={item.id}
                  href={`#${item.id}`}
                  onClick={() => setActiveSection(item.id)}
                  className={`group relative px-3 xl:px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${
                    tokens.typography.label
                  } ${
                    scrolled 
                      ? activeSection === item.id
                        ? 'text-orange-600'
                        : 'text-gray-700 hover:text-orange-600'
                      : activeSection === item.id
                        ? 'text-orange-400'
                        : 'text-white hover:text-orange-400'
                  } ${tokens.animations.basic.fadeIn}`}
                  style={{ 
                    animationDelay: `${index * 0.05}s`,
                  }}
                >
                  <span className="relative z-10">{item.label}</span>
                  
                  {/* Active Indicator */}
                  {activeSection === item.id && (
                    <div className={`absolute -bottom-1 left-1/2 -translate-x-1/2 w-1 h-1 rounded-full ${
                      scrolled ? 'bg-orange-600' : 'bg-orange-400'
                    } shadow-lg ${scrolled ? 'shadow-orange-600/50' : 'shadow-orange-400/50'}`}>
                      <div className="absolute inset-0 rounded-full animate-ping opacity-75"></div>
                    </div>
                  )}
                  
                  {/* Hover Background */}
                  <div className={`absolute inset-0 rounded-lg opacity-0 group-hover:opacity-100 transition-opacity duration-300 ${
                    scrolled ? 'bg-orange-50' : 'bg-white/10'
                  }`}></div>
                </a>
              ))}
            </div>

            {/* Right Side Actions */}
            <div className="flex items-center gap-2 sm:gap-3">
              {/* Language Selector - Using typography.label */}
              <div className="relative">
                <button
                  onClick={() => setShowLangMenu(!showLangMenu)}
                  className={`flex items-center gap-1.5 sm:gap-2 px-2.5 sm:px-3 py-1.5 sm:py-2 rounded-lg font-semibold transition-all duration-300 ${
                    tokens.typography.label
                  } ${
                    scrolled
                      ? 'text-gray-700 bg-gray-100 hover:bg-orange-50 hover:text-orange-600'
                      : 'text-white bg-white/10 hover:bg-white/20 backdrop-blur-md'
                  }`}
                >
                  <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129" />
                  </svg>
                  <span className="hidden sm:inline">{language}</span>
                  <svg className={`w-3 h-3 transition-transform duration-300 ${showLangMenu ? 'rotate-180' : ''}`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
                  </svg>
                </button>

                {/* Language Dropdown - Using animations.slideUp */}
                {showLangMenu && (
                  <>
                    <div className="fixed inset-0 z-40" onClick={() => setShowLangMenu(false)}></div>
                    <div className={`absolute top-full right-0 mt-2 w-44 rounded-xl shadow-2xl border overflow-hidden z-50 ${
                      scrolled
                        ? 'bg-white border-gray-200'
                        : 'bg-slate-900/95 backdrop-blur-2xl border-white/20'
                    } ${tokens.animations.basic.slideUp}`}>
                      {languages.map((lang) => (
                        <button
                          key={lang.code}
                          onClick={() => {
                            setLanguage(lang.code);
                            setShowLangMenu(false);
                          }}
                          className={`w-full flex items-center gap-3 px-3 py-2.5 text-left transition-all duration-200 ${
                            language === lang.code
                              ? scrolled
                                ? 'bg-orange-50 text-orange-600'
                                : 'bg-orange-500/20 text-orange-400'
                              : scrolled
                                ? 'text-gray-700 hover:bg-gray-50'
                                : 'text-white hover:bg-white/10'
                          }`}
                        >
                          <span className="text-xl">{lang.flag}</span>
                          <div className="flex-1">
                            <div className={`font-semibold ${tokens.typography.label}`}>{lang.code}</div>
                            <div className={`${tokens.typography.caption} ${
                              language === lang.code
                                ? scrolled ? 'text-orange-500' : 'text-orange-300'
                                : scrolled ? 'text-gray-500' : 'text-gray-400'
                            }`}>{lang.name}</div>
                          </div>
                          {language === lang.code && (
                            <svg className="w-4 h-4 text-orange-500" fill="currentColor" viewBox="0 0 20 20">
                              <path fillRule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clipRule="evenodd" />
                            </svg>
                          )}
                        </button>
                      ))}
                    </div>
                  </>
                )}
              </div>

              {/* Mobile Menu Button */}
              <button 
                onClick={() => setIsOpen(true)}
                className={`lg:hidden relative p-2 rounded-lg transition-all duration-300 ${
                  scrolled 
                    ? 'text-gray-700 bg-gray-100 hover:bg-orange-50 hover:text-orange-600' 
                    : 'text-white bg-white/10 hover:bg-white/20 backdrop-blur-md'
                }`}
                aria-label="Open menu"
              >
                <svg className="w-5 h-5 sm:w-6 sm:h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth={2.5}>
                  <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
              </button>
            </div>
          </div>
        </div>

        {/* Progress Bar */}
        {scrolled && (
          <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-gray-200">
            <div 
              className="h-full bg-gradient-to-r from-orange-500 via-pink-500 to-orange-500 transition-all duration-300"
              style={{ 
                width: `${Math.min((lastScrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100, 100)}%` 
              }}
            ></div>
          </div>
        )}
      </nav>

      {/* Mobile Menu */}
      {isOpen && (
        <>
          {/* Backdrop */}
          <div 
            className="lg:hidden fixed inset-0 z-[60] bg-black/70 backdrop-blur-sm transition-opacity duration-300"
            onClick={() => setIsOpen(false)}
          ></div>
          
          {/* Menu Panel - Using animations.slideUp */}
          <div className={`lg:hidden fixed top-0 right-0 bottom-0 z-[70] w-full max-w-sm bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 shadow-2xl transform transition-transform duration-500 ease-out overflow-y-auto ${tokens.animations.basic.slideUp}`}>
            {/* Header */}
            <div className="sticky top-0 bg-slate-900/95 backdrop-blur-xl border-b border-white/10 p-4 sm:p-6 z-10">
              <div className="flex items-center justify-between mb-4">
                {/* Using gradient-text-orange from CSS */}
                <span className={`gradient-text-orange font-bold ${tokens.typography.h3}`}>
                  PT. GLOBALINDO
                </span>
                <button 
                  onClick={() => setIsOpen(false)}
                  className="p-2 rounded-lg text-white hover:bg-white/10 transition-all duration-300 hover:rotate-90"
                  aria-label="Close menu"
                >
                  <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth={2.5}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
                  </svg>
                </button>
              </div>
              
              {/* Mobile Language Selector */}
              <div className="flex gap-2">
                {languages.map((lang) => (
                  <button
                    key={lang.code}
                    onClick={() => setLanguage(lang.code)}
                    className={`flex-1 flex flex-col items-center gap-1 p-2.5 rounded-lg transition-all duration-300 ${
                      language === lang.code
                        ? 'bg-orange-500/20 border border-orange-500/50'
                        : 'bg-white/5 border border-white/10 hover:bg-white/10'
                    }`}
                  >
                    <span className="text-xl">{lang.flag}</span>
                    <span className={`font-semibold ${tokens.typography.caption} ${
                      language === lang.code ? 'text-orange-400' : 'text-white/70'
                    }`}>{lang.code}</span>
                  </button>
                ))}
              </div>
            </div>

            {/* Menu Items - Using typography.bodyBase and animations.fadeIn */}
            <div className="p-4 sm:p-6 space-y-1">
              {menuItems.map((item, index) => (
                <a 
                  key={item.id}
                  href={`#${item.id}`} 
                  onClick={() => {
                    setActiveSection(item.id);
                    setIsOpen(false);
                  }}
                  className={`group flex items-center justify-between p-4 rounded-xl transition-all duration-300 ${
                    activeSection === item.id
                      ? 'bg-orange-500/20 text-orange-400'
                      : 'text-white hover:bg-white/10'
                  } ${tokens.animations.basic.fadeIn}`}
                  style={{
                    animationDelay: `${index * 0.05}s`,
                  }}
                >
                  <span className={`font-semibold ${tokens.typography.bodyBase}`}>{item.label}</span>
                  
                  <svg className={`w-5 h-5 transform transition-all duration-300 ${
                    activeSection === item.id ? 'translate-x-1 text-orange-400' : 'text-gray-500 group-hover:translate-x-1 group-hover:text-orange-400'
                  }`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                  </svg>
                </a>
              ))}
            </div>
            
            {/* Footer - Using typography.caption */}
            <div className="p-4 sm:p-6 border-t border-white/10 mt-auto">
              <p className={`text-white/50 text-center mb-3 ${tokens.typography.caption}`}>Follow Us</p>
              <div className="flex justify-center gap-3 mb-4">
                {[
                  { icon: "M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z" },
                  { icon: "M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z" },
                  { icon: "M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z" }
                ].map((social, i) => (
                  <a key={i} href="#" className="w-10 h-10 rounded-lg bg-white/10 hover:bg-orange-500/20 border border-white/10 hover:border-orange-500/50 flex items-center justify-center transition-all duration-300 hover:scale-110">
                    <svg className="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 24 24">
                      <path d={social.icon}/>
                    </svg>
                  </a>
                ))}
              </div>
              <p className={`text-white/30 text-center ${tokens.typography.caption}`}>© 2025 PT. Globalindo Intimates</p>
            </div>
          </div>
        </>
      )}
    </>
  );
};

export default Navigation;